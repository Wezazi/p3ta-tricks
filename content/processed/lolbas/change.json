{"title": "Change.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "change", "html": "<h1>Change.exe</h1>\n<p>Remote Desktop Services MultiUser Change Utility</p>\n<h2>Paths</h2><ul>\n<li><code>c:\\windows\\system32\\change.exe</code></li>\n<li><code>c:\\windows\\syswow64\\change.exe</code></li>\n</ul>\n<h2>Commands</h2>\n<h3>Execute</h3>\n<p>Once executed, `change.exe` will execute `chgusr.exe` in the same folder. Thus, if `change.exe` is copied to a folder and an arbitrary executable is renamed to `chgusr.exe`, `change.exe` will spawn it. Instead of `user`, it is also possible to use `port` or `logon` as command-line option.</p>\n<p><em>Use case: Execute an arbitrary executable via trusted system executable.</em></p>\n<p><em>Privileges: User</em></p>\n<pre><code class=\"language-bash\">change.exe user</code></pre>\n<h2>Detection</h2><ul>\n<li>IOC: <a href=\"change.exe being executed and executes a child process outside of its normal path of c:\\windows\\system32\\ or c:\\windows\\syswow64\\\">change.exe being executed and executes a child process outside of its normal path of c:\\windows\\system32\\ or c:\\windows\\syswow64\\</a></li>\n</ul>\n", "excerpt": "Change.exe Remote Desktop Services MultiUser Change Utility Paths c:\\windows\\system32\\change.exe c:\\windows\\syswow64\\change.exe Commands Execute Once executed, `change.exe` will execute `chgusr.exe` i", "tags": ["Execute"]}