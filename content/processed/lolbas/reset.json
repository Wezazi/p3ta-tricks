{"title": "Reset.exe", "source": "lolbas", "source_label": "LOLBAS", "path": "reset", "html": "<h1>Reset.exe</h1>\n<p>Remote Desktop Services Reset Utility</p>\n<h2>Paths</h2><ul>\n<li><code>c:\\windows\\system32\\reset.exe</code></li>\n<li><code>c:\\windows\\syswow64\\reset.exe</code></li>\n</ul>\n<h2>Commands</h2>\n<h3>Execute</h3>\n<p>Once executed, `reset.exe` will execute `rwinsta.exe` in the same folder. Thus, if `reset.exe` is copied to a folder and an arbitrary executable is renamed to `rwinsta.exe`, `reset.exe` will spawn it.</p>\n<p><em>Use case: Execute an arbitrary executable via trusted system executable.</em></p>\n<p><em>Privileges: User</em></p>\n<pre><code class=\"language-bash\">reset.exe session</code></pre>\n<h2>Detection</h2><ul>\n<li>IOC: <a href=\"reset.exe being executed and executes rwinsta.exe outside of its normal path of c:\\windows\\system32\\ or c:\\windows\\syswow64\\\">reset.exe being executed and executes rwinsta.exe outside of its normal path of c:\\windows\\system32\\ or c:\\windows\\syswow64\\</a></li>\n</ul>\n", "excerpt": "Reset.exe Remote Desktop Services Reset Utility Paths c:\\windows\\system32\\reset.exe c:\\windows\\syswow64\\reset.exe Commands Execute Once executed, `reset.exe` will execute `rwinsta.exe` in the same fol", "tags": ["Execute"]}